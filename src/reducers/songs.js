import * as Types from '../actiontypes/singer'

export default function songs(state = [], action) {
    switch (action.type) {
        case Types.ADD_SONG:
        
        default:
            return state;
    }
}